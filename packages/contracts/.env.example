# =============================================================================
# EscrowFlow — Hardhat / contracts (`packages/contracts`)
# Copy to `packages/contracts/.env` for deploy scripts (never commit secrets).
# =============================================================================

# --- Testnets (sepolia, baseSepolia, arbitrumSepolia) ---
DEPLOYER_PRIVATE_KEY=
SEPOLIA_RPC_URL=
BASE_SEPOLIA_RPC_URL=
ARBITRUM_SEPOLIA_RPC_URL=

# --- Registry deploy (deploy-registry.ts, deploy-stack.ts) ---
# REGISTRY_ADMIN_ADDRESS=
# ARBITRATOR_ADDRESS=

# --- Explorer verification (optional; add hardhat-verify plugin first) ---
# ETHERSCAN_API_KEY=
# BASESCAN_API_KEY=
# ARBISCAN_API_KEY=
