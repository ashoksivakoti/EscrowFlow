# EscrowFlow — contracts deployment

Legacy deployment flows were removed. `EscrowFlowRegistry` now deploys only through the canonical path.

Use:

- `docs/canonical-escrowflowregistry-deployment.md`
