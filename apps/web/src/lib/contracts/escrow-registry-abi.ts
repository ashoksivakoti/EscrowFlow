/**
 * @deprecated Import from `@/lib/contracts/escrow-registry-abi.full` directly.
 * This shim remains for compatibility while callers are migrated.
 */
export { escrowRegistryAbi } from "@/lib/contracts/escrow-registry-abi.full";
